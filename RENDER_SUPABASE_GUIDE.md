# Render Deployment with Supabase - Quick Guide

## المشكلة | The Problem

عند استخدام Render على الخطة المجانية:
- ❌ يتم إيقاف الخادم بعد 15 دقيقة من عدم النشاط
- ❌ تُحذف البيانات المخزنة في SQLite عند إعادة التشغيل
- ❌ لا يمكن الاحتفاظ بالملفات المرفوعة

## الحل | The Solution

استخدام **Supabase** كقاعدة بيانات خارجية دائمة:
- ✅ البيانات تبقى حتى عند إعادة تشغيل Render
- ✅ مجاني حتى 500MB تخزين
- ✅ يعمل مع Docker الحالي بدون تعديلات كبيرة

---

## خطوات الإعداد السريع | Quick Setup

### 1. إنشاء قاعدة بيانات Supabase

1. اذهب إلى [supabase.com](https://supabase.com)
2. سجل دخول أو أنشئ حساب جديد
3. اضغط **"New Project"**
4. املأ البيانات:
   - **Name**: `saha-platform`
   - **Database Password**: اختر كلمة مرور قوية واحفظها!
   - **Region**: اختر الأقرب لك (مثل: Frankfurt)
5. انتظر 2-3 دقائق حتى يتم إنشاء المشروع

### 2. الحصول على رابط الاتصال

1. من لوحة تحكم Supabase، اذهب إلى:
   **Settings** → **Database** → **Connection String** → **URI**

2. انسخ الرابط وسيكون بهذا الشكل:
   ```
   postgresql://postgres.xxxxx:[YOUR-PASSWORD]@aws-0-eu-central-1.pooler.supabase.com:5432/postgres
   ```

3. **مهم جداً**: استبدل `[YOUR-PASSWORD]` بكلمة المرور التي اخترتها في الخطوة 1

### 3. تحديث متغيرات البيئة في Render

1. اذهب إلى لوحة تحكم Render: [dashboard.render.com](https://dashboard.render.com)
2. اختر خدمتك (Web Service)
3. اذهب إلى **Environment**
4. أضف أو حدّث المتغير التالي:

   ```
   Key: DATABASE_URL
   Value: postgresql://postgres.xxxxx:[YOUR-PASSWORD]@aws-0-eu-central-1.pooler.supabase.com:5432/postgres
   ```

5. احفظ التغييرات

### 4. إعادة نشر التطبيق

1. في Render، اضغط **"Manual Deploy"** → **"Deploy latest commit"**
2. انتظر حتى يكتمل البناء (5-10 دقائق)
3. تحقق من اللوجات (Logs) للتأكد من الاتصال بقاعدة البيانات

---

## التحقق من نجاح الإعداد | Verification

### في لوجات Render، يجب أن ترى:

```
🚀 Starting Saha Platform...
📊 Using PostgreSQL (Supabase) database...
🔄 Running database migrations...
✅ Database schema created
🌱 Seeding database with initial data...
✅ Database seeded
✅ Database ready!
🌐 Starting application...
```

### اختبار التطبيق:

1. افتح رابط تطبيقك على Render
2. سجل مستخدم جديد
3. أنشئ إعلان
4. أعد تشغيل الخدمة في Render (Manual Deploy)
5. **تحقق**: يجب أن يبقى المستخدم والإعلان موجودين! ✅

---

## متغيرات البيئة الكاملة | Full Environment Variables

في Render Environment، تأكد من وجود:

```env
# Database (Supabase)
DATABASE_URL=postgresql://postgres.xxxxx:[PASSWORD]@aws-0-eu-central-1.pooler.supabase.com:5432/postgres

# JWT Secret
JWT_SECRET=saha_platform_secret_key_secure_2025_change_this_in_production

# Node Environment
NODE_ENV=production

# Port (Render يحدده تلقائياً)
PORT=5000
```

---

## استكشاف الأخطاء | Troubleshooting

### خطأ: "Can't reach database server"

**الحل**:
1. تأكد من صحة رابط DATABASE_URL
2. تأكد من استبدال `[YOUR-PASSWORD]` بكلمة المرور الفعلية
3. تحقق من أن المشروع نشط في Supabase

### خطأ: "SSL connection required"

**الحل**: أضف `?sslmode=require` لنهاية DATABASE_URL:
```
postgresql://...postgres?sslmode=require
```

### البيانات لا تزال تُحذف

**الحل**:
1. تحقق من أن DATABASE_URL في Render يشير إلى Supabase (وليس SQLite)
2. تأكد من أن Dockerfile يكتشف PostgreSQL بشكل صحيح
3. راجع اللوجات للتأكد من رسالة "Using PostgreSQL (Supabase) database..."

---

## المزايا الإضافية | Additional Benefits

### 1. لوحة تحكم Supabase

يمكنك إدارة البيانات مباشرة:
- **Table Editor**: عرض وتعديل البيانات
- **SQL Editor**: تنفيذ استعلامات مخصصة
- **Database**: مراقبة الأداء والاتصالات

### 2. النسخ الاحتياطي التلقائي

Supabase يقوم بنسخ احتياطي يومي تلقائي لقاعدة البيانات.

### 3. قابلية التوسع

يمكنك الترقية لاحقاً للحصول على:
- مساحة تخزين أكبر
- أداء أفضل
- نسخ احتياطية أكثر تكراراً

---

## البدائل | Alternatives

إذا كنت تفضل خدمات أخرى:

### 1. Neon (neon.tech)
- مجاني حتى 3GB
- PostgreSQL serverless
- سريع جداً

### 2. Railway (railway.app)
- قاعدة بيانات مدمجة
- سهل الإعداد
- خطة مجانية محدودة

### 3. PlanetScale (planetscale.com)
- MySQL serverless
- يتطلب تعديل Prisma schema

---

## الخلاصة | Summary

✅ **Supabase + Render = حل مثالي للخطة المجانية**

- قاعدة بيانات دائمة
- لا حاجة لتغيير Docker
- سهل الإعداد والصيانة
- قابل للتوسع مستقبلاً

**الوقت المتوقع للإعداد**: 10-15 دقيقة

---

**للمزيد من التفاصيل**: راجع [`SUPABASE_SETUP.md`](SUPABASE_SETUP.md)

**تم إنشاؤه بواسطة**: Saha Platform Team  
**التاريخ**: 2026-01-15
